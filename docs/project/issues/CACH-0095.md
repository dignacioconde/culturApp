---
schema_version: 2
kind: issue
id: CACH-0095
title: Validadores de policies documentales del Product Brain
lifecycle: active
created: '2026-05-18'
updated: '2026-05-18'
aliases:
  - CACH-0095
tags:
  - product-brain
  - issue
  - retrieval
  - governance
generated: false
work_type: chore
work_level: task
issue_workflow: backlog
priority: p2
size: s
area: brain
components:
  - product-brain
  - agents
parent: CACH-B0010
related: []
depends_on: []
blocked_by: []
adr:
  - ADR-0010
release: null
theme: internal-agent-ops
---
# CACH-0095 — Validadores de policies documentales del Product Brain

## Objetivo

Añadir validadores ligeros para que las policies documentales introducidas en Fase 1 sean auditables sin convertir el Product Brain en una arquitectura RAG pesada.

## Alcance

Incluido:

- Validar valores opcionales de `load_policy`, `index_policy` y `context_type` cuando estén presentes.
- Detectar prompts históricos que podrían cargarse por error.
- Detectar documentos `deprecated`, `historical` o `archived` tratados como contexto activo.
- Detectar usos de issues `done` o releases `released` como contexto vivo cuando no haya referencia explícita.
- Mantener los campos nuevos como opcionales; no anotar masivamente documentos existentes.

Fuera de alcance:

- Vector DB, embeddings o chunk index.
- Retrieval profiles ejecutables.
- Renombrado de skills o agentes.
- Cambios en `src/`.

## Criterios de aceptacion

- [ ] `pb:check` o un script acotado detecta valores inválidos de `load_policy`, `index_policy` y `context_type` cuando aparecen.
- [ ] El check distingue documentos excluidos por defecto de documentos realmente rotos, sin fallar por la ausencia de campos opcionales.
- [ ] Los prompts históricos, issues `done`, releases `released` y docs deprecated/historical/archived quedan cubiertos por reglas verificables.
- [ ] No se genera chunk index ni se introduce dependencia de embeddings o servicios externos.
- [ ] `src/` sigue desacoplado del Product Brain.

## Plan tecnico

Extender los checks existentes en `scripts/brain/` o añadir un script local acotado si resulta más claro. Reutilizar el schema v2 y el `document-types-registry` como contrato humano. Evitar migraciones masivas: el primer paso debe validar comportamiento y detectar riesgos, no rellenar metadata en todo el Brain.

## Validacion

- `npm run pb:check -- --strict --json`
- `npm run verify:brain`
- `npm run context:check`
- `npm run pb:guard`
- `git diff --check`

## Resultado

Pendiente hasta cerrar la issue.

## Desarrollo

- Rama:
- PR:
- Estado actual: backlog.

## Notas de progreso

Issue creada para separar Fase 2 de la Fase 1 documental.

## Cambios de alcance y decisiones

No aplica por ahora.

## Bloqueos

No aplica por ahora.

## Validación ejecutada

Pendiente hasta ejecutar la issue.

## Memoria

Memoria: no aplica.
