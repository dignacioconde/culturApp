---
schema_version: 2
kind: issue
id: CACH-0067
title: PWA instalable basica y navegacion standalone
lifecycle: active
created: '2026-05-13'
updated: '2026-05-13'
aliases:
  - CACH-0067
tags:
  - product-brain
  - issue
  - pwa
  - mobile
generated: false
work_type: feature
work_level: slice
issue_workflow: done
priority: p1
size: s
area: infra
components:
  - infra-deploy
parent: CACH-B0008
related: []
depends_on: []
blocked_by: []
adr: []
release: RELEASE-0.1.0-beta.21
theme: beta-trust
---
# CACH-0067 — PWA instalable basica y navegacion standalone

## Objetivo

Hacer que Cachés sea instalable como PWA basica y que al abrirse desde el icono mantenga la navegacion interna sin barras del navegador.

## Alcance

- Manifest con `display: standalone`, `scope: "/"`, `start_url`, colores e iconos.
- Meta tags PWA y Apple en `index.html`.
- Service worker para app shell y assets estaticos.
- Smoke postdeploy ampliado para manifest y service worker.

Fuera de alcance: push, notificaciones, recordatorios, resumen semanal y offline de datos con conflictos.

## Criterios de aceptacion

- [x] `/manifest.webmanifest` declara standalone, root scope e iconos 192/512/maskable.
- [x] `sw.js` cachea solo app shell/assets same-origin.
- [x] No se cachean Supabase Auth, REST, Edge Functions ni datos de usuario.
- [x] Se conserva el rewrite SPA de Vercel.
- [x] Verificacion iPhone/Android queda documentada como smoke manual humano tras deploy; el agente verifica manifest, `sw.js` y rutas SPA.

## Plan tecnico

Usar assets en `public/`, registrar service worker solo en builds de produccion y mantener la estrategia network-first para navegacion.

## Validacion

- E2E de manifest.
- `npm run build`
- `npm run smoke:postdeploy -- --url=https://app.caches.es` tras deploy.
- Smoke manual iPhone Safari y Android Chrome.

## Resultado

Implementado y cerrado en `RELEASE-0.1.0-beta.21`. Cachés sirve `manifest.webmanifest`, iconos 192/512/maskable, meta tags PWA/Apple, `viewport-fit=cover` y un service worker de app shell que excluye Supabase/Auth/REST/Edge Functions y datos de usuario.

## Desarrollo

- Rama: `release/0.1.0-beta.21`
- PR: https://github.com/dignacioconde/culturApp/pull/108
- Estado actual: done.

## Validación ejecutada

- `npm run lint` OK.
- `npm run test` OK: 28 archivos, 156 tests.
- `npm run build` OK, con aviso no bloqueante de chunk grande de Vite.
- `npm run test:e2e` OK: 10 passed, 1 skipped.
- `npm run smoke:postdeploy -- --url=http://127.0.0.1:4173` OK sobre `vite preview`: rutas SPA, manifest y `sw.js`.
- `npm run pb:check` OK.
- `npm run pb:guard` OK.

## Memoria

Memoria: actualizada en `.memory/projects/routing-deploy.md` con la regla PWA basica: scope `/`, rewrite SPA conservado y no cachear Supabase/Auth/REST/Edge Functions ni datos de usuario.
